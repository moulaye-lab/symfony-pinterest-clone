{% extends 'base.html.twig' %}

{% block title %} {{pin.title}} {% endblock %}

{% block body %}
    
   
    
        <article>
            <h1>{{pin.title}}</a></h1>
            <p>{{pin.description }}</p>
        </article>
         <a href ="{{path ('app_home')}}">Go Back</a>
         <a href ="{{ path('app_pin_edit' , {'id': pin.id }) }}" class="btn btn-primary">Edit </a>

{% endblock %}
